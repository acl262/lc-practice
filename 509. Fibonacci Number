class Solution {
    public int fib(int N) {
        if(N == 0) return 0;
        if(N == 1) return 1;
        int rtn = 0;
        if(N >1 ) {
            rtn = fib(N-1) + fib(N-2);
        }
        return rtn; 
    }
}
